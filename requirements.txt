# Core
fastapi>=0.115.0
uvicorn>=0.44.0
pydantic>=2.10.0

# LLM
litellm>=1.52.0
tiktoken>=0.8.0

# Memory / Vector Store
chromadb>=0.6.0

# Analysis
pyyaml>=6.0.2

# Code quality
ruff>=0.15.0

# CLI
typer>=0.12.0
rich>=13.9.0

# Async
aiofiles>=24.1.0
httpx>=0.28.0

# Optional ecosystem integrations (Tier 1 — install to unlock bridge features)
# code2llm   — PERCEIVE: multi-language analysis, call graph, toon.yaml generation
#              pip install code2llm
# regix      — VALIDATE: regression detection, quality gates, metric snapshots
#              pip install regix
# vallm      — REFLECT: multi-validator patch pipeline, security, semantic scoring
#              pip install vallm
# redup      — Phase 2: exact/structural duplicate detection
#              pip install redup==0.4.18
