# code2llm/evolution | 568 func | 79f | 2026-04-08

NEXT[1] (ranked by impact):
  [1] !  SPLIT-FUNC      apply_changes_with_llm_supervision  CC=16  fan=19
      WHY: CC=16 exceeds 15
      EFFORT: ~1h  IMPACT: 304


RISKS[0]: none

METRICS-TARGET:
  CC̄:          3.9 → ≤2.7
  max-CC:      16 → ≤8
  god-modules: 0 → 0
  high-CC(≥15): 1 → ≤0
  hub-types:   0 → ≤0

PATTERNS (language parser shared logic):
  _extract_declarations() in base.py — unified extraction for:
    - TypeScript: interfaces, types, classes, functions, arrow funcs
    - PHP: namespaces, traits, classes, functions, includes
    - Ruby: modules, classes, methods, requires
    - C++: classes, structs, functions, #includes
    - C#: classes, interfaces, methods, usings
    - Java: classes, interfaces, methods, imports
    - Go: packages, functions, structs
    - Rust: modules, functions, traits, use statements

  Shared regex patterns per language:
    - import: language-specific import/require/using patterns
    - class: class/struct/trait declarations with inheritance
    - function: function/method signatures with visibility
    - brace_tracking: for C-family languages ({ })
    - end_keyword_tracking: for Ruby (module/class/def...end)

  Benefits:
    - Consistent extraction logic across all languages
    - Reduced code duplication (~70% reduction in parser LOC)
    - Easier maintenance: fix once, apply everywhere
    - Standardized FunctionInfo/ClassInfo models

HISTORY:
  2026-04-08: Split redsl/awareness/git_timeline.py → 4 modules (timeline_models, timeline_analysis, timeline_git, timeline_toon)
  2026-04-08: Split redsl/execution/executor.py → 4 modules (cycle, decision, resolution, validation, sandbox_execution)
  2026-04-08: Split redsl/refactors/direct.py → 4 modules (direct_imports, direct_guard, direct_constants, direct_types)
  prev CC̄=4.1 → now CC̄=3.9, god-modules: 3 → 0, high-CC≥15: reduced
